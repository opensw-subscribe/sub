apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.37.0 (HEAD)
  labels:
    io.kompose.service: backend
  name: backend
spec:
  type: NodePort
  selector:
    io.kompose.service: backend
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
      nodePort: 30080